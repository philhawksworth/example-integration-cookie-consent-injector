# yaml-language-server: $schema=https://sdk.netlify.com/sdk/config-schema.json
config:
  slug: cookie-consent-injector-example
  name: cookie-consent-injector-example
  description: Intelligently inject cookie banner HTML when required
  integrationLevel: site
  scopes:
    site: ["read", "write"]
